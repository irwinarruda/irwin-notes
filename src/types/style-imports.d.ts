// Allow side-effect style imports (for example `import "@/styles/globals.css"`).
// Some editors enable `noUncheckedSideEffectImports`, which requires these modules
// to have declarations.
declare module "*.css";
